# dunder == double under (underscores)
# run -> edit configurations -> "+" -> python -> script path -> save
# use to if __name__ == "__main__" to check if a file/script is currently being run script1 and 2 can independently be run
# because they have their own main method and there is an if statement that checks it